imports:
    - { resource: ../config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

services:
    _defaults:
        autowire: true
        autoconfigure: true